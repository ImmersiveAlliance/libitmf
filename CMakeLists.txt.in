cmake_minimum_required(VERSION 2.8.2)

project(expected-download NONE)

include(ExternalProject)
ExternalProject_Add(expected
  GIT_REPOSITORY    https://github.com/TartanLlama/expected.git
  GIT_TAG           master
  SOURCE_DIR        "${CMAKE_CURRENT_BINARY_DIR}/expected-src"
  BINARY_DIR        "${CMAKE_CURRENT_BINARY_DIR}/expected-build"
  CONFIGURE_COMMAND ""
  BUILD_COMMAND     ""
  INSTALL_COMMAND   ""
  TEST_COMMAND      ""
)