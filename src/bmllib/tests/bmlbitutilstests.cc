#include "gtest/gtest.h"
#include "../core/bmlbitutils.h"

namespace bmllib {

	TEST(LeftShiftTest, HandlesSingleByte) {
		EXPECT_EQ(0, 0); // test to see if CTest working
	}

	TEST(LeftShiftTest, HandlesSingleByte2) {
		EXPECT_EQ(0, 0); // test to see if CTest working
	}

}